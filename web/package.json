{
  "name": "fl-monitor-dashboard",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=static/dashboard.js --minify --format=esm && shx cp src/index.html static/ && shx cp src/style.css static/",
    "watch": "esbuild src/main.ts --bundle --outfile=static/dashboard.js --format=esm --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "shx": "^0.4.0",
    "typescript": "^5.7.0"
  }
}
