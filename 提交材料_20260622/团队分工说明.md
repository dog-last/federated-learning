# 团队分工说明

> 本分工基于仓库 Git 提交历史、文件变更记录和项目最终功能整理。

## 1. 林子淳

主要负责实验报告、结果分析、可视化材料、海报制作和后期修复整合。

完成内容：

- 编写并完善测试报告，整理 Centralized、Ring Federated、SplitFed 三种模式的训练结果。
- 整理准确率曲线、损失曲线、通信开销对比图等实验可视化材料。
- 添加 `scripts/full_10_round_comparison.py`，用于自动筛选或补跑三种模式的 10 轮实验，并生成对比图。
- 修复 MNIST 数据下载稳定性问题。
- 修复项目根目录默认路径问题，保证 `manager.py`、server、client、ring node 在不同启动方式下能正确定位数据。
- 修复 SplitFed 指标口径问题：客户端不再上报基于旧 shadow server model 的近似 val/test 指标，报告主指标统一使用服务端 global evaluation。
- 添加日志筛选相关前端功能。
- 调整配置文件、训练超时时间和报告图片路径，保证测试报告中的图片可正确渲染。
- 整理提交材料，包括源码、文档、报告、图片和项目总结。

对应代表性文件：

- `source/core/client.py`
- `source/core/server.py`
- `source/core/ring_node.py`
- `source/scripts/prepare_mnist.py`
- `source/scripts/full_10_round_comparison.py`
- `reports/测试报告.md`
- `images/`
- `项目工作总结.md`

## 2. 林一旦

主要负责项目早期框架、核心训练流程、Web 监控框架和文档基础建设。

完成内容：

- 搭建项目基础结构，迁移到 uv/pyproject 管理方式。
- 实现早期 server-client 训练框架和 P2P/Ring 相关基础逻辑。
- 完成核心模块、测试模块和基础文档的早期建设。
- 实现 FastAPI 监控接口，提供 `/api/state`、`/api/metrics/history` 等监控数据接口。
- 实现 Web render mode，使训练过程可以通过 Web 仪表盘查看。
- 实现 manager 对 Web 模式的支持。
- 将客户端 val/test 指标随训练 payload 上传，为后续监控和报告统计提供数据基础。
- 清理不应进入仓库的 node_modules 等生成目录。
- 更新配置说明和监控说明文档。

对应代表性文件：

- `source/manager.py`
- `source/core/client.py`
- `source/core/server.py`
- `source/utils/monitor_api.py`
- `source/utils/monitoring.py`
- `docs/configuration.md`
- `docs/monitoring.md`
- `docs/overview.md`
- `web/`

## 3. 陈明松

主要负责日志系统、监控增强、Ring 模式完善、超时处理和运行产物管理。

完成内容：

- 完善 Ring 模式机制，修复 Ring 模式下 tx/rx 显示为 0 的问题。
- 增强训练日志系统，引入 run_id、run_log_dir 和按运行时间区分的日志目录。
- 调整训练图表自动保存目录，避免不同实验结果互相覆盖。
- 增加 monitor summary、runtime log record、metrics history 等运行摘要能力。
- 重构 Web dashboard 中 runtime logs、event log 和 chart 数据处理逻辑。
- 完善服务端超时处理和掉队节点处理逻辑。
- 增加日志级别环境变量配置和 debug logging。
- 更新 README、overview、ring-mode、monitoring 等文档中的日志路径和使用说明。

对应代表性文件：

- `source/core/server.py`
- `source/core/ring_node.py`
- `source/utils/monitor_api.py`
- `source/utils/monitoring.py`
- `source/utils/training_controller.py`
- `docs/ring-mode.md`
- `docs/overview.md`
- `docs/monitoring.md`
- `web/src/dashboard.ts`
- `web/src/event-log.ts`
- `web/src/charts.ts`

## 4. 刘竞泽

主要负责项目初始化、数据路径修复、Proposal 和展示材料。

完成内容：

- 完成项目初始提交和部分无用文件清理。
- 修复数据路径定位问题，使模块能在不同目录结构下正确找到数据文件。
- 编写开放问题 Proposal。
- 添加展示材料和技术汇报材料。
- 稳定监控展示相关内容，补充 presentation 目录下的报告、讲稿和 slides。
- 配合后期监控展示与演示材料整合。

对应代表性文件：

- `docs/Proposal.md`
- `presentation/技术报告.md`
- `presentation/展示主旨与讲稿.md`
- `presentation/main.tex`
- `presentation/main.pdf`
- `source/core/client.py`
- `source/core/server.py`


